import React from 'react';
import PropTypes from 'prop-types';

Custom.propTypes = {
    
};

function Custom(props) {
    return (
        <div>
            
        </div>
    );
}

export default Custom;