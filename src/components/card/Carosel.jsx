import React from 'react';
import PropTypes from 'prop-types';

Carosel.propTypes = {
    
};

function Carosel(props) {
    return (
        <div>
            carosel work
        </div>
    );
}

export default Carosel;